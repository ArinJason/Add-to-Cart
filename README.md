# Add-to-Cart
A ReactJS application that fetches products from the Fake Store API and displays them in a responsive layout. Users can add items to a cart, view cart count in the navbar, manage items through a modal, prevent duplicate additions, and remove products from the cart.
